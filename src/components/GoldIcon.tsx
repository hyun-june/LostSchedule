import { Image } from "react-native";

const GoldIcon = () => {
  return (
    <Image
      source={{ uri: "https://loawa.com/assets/statics/icons/gold.png" }}
      style={{
        width: 10,
        height: 10,
      }}
    ></Image>
  );
};

export default GoldIcon;
